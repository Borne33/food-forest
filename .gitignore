# ── secrets: never commit ──
.env
importer/.env

# ── python ──
__pycache__/
*.pyc
.venv/
venv/

# ── importer: regenerable scratch (drafts/ are kept as curated source) ──
importer/packets/

# ── os ──
.DS_Store
